#include <iostream>
using namespace std;

int T,W;
int dp[1001][31];

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    cin >> T >> W;
    for(int i=0;i<T;i++) {
        int cur;
        cin >> cur;
        dp[i+1][]
    }
}