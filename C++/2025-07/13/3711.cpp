#include <iostream>
using namespace std;

int stu[300];

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int T;
    cin >> T;
    while(T--) {
        int n;
        cin >> n;
        for(int i=0;i<n;i++) cin >> stu[i];
        int m=1;
        while (true) {
            

        }
    }
}